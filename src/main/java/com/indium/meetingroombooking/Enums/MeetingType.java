package com.indium.meetingroombooking.Enums;

/**
 * Enum representing the types of meetings that can be scheduled.
 */
public enum MeetingType {

    /** Meetings that are conducted within the organization. */
    INTERNAL,

    /** Meetings that involve external participants or entities. */
    EXTERNAL;
}
